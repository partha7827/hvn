import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:highvibe/modules/profile/broadcast/broadcast_page.dart';

main() {
  testWidgets('BroadcastPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(BroadcastPage(title: 'Broadcast')));
    //  final titleFinder = find.text('Broadcast');
    //  expect(titleFinder, findsOneWidget);
  });
}
