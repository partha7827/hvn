import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:highvibe/app/tribe/tribe_page.dart';

main() {
  testWidgets('TribePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TribePage(title: 'Tribe')));
    //  final titleFinder = find.text('Tribe');
    //  expect(titleFinder, findsOneWidget);
  });
}
