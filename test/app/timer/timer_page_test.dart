import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:highvibe/app/timer/timer_page.dart';

main() {
  testWidgets('TimerPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TimerPage(title: 'Timer')));
    //  final titleFinder = find.text('Timer');
    //  expect(titleFinder, findsOneWidget);
  });
}
