import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:highvibe/app/timer/timer_controller.dart';
import 'package:highvibe/app/timer/timer_module.dart';

void main() {
  initModule(TimerModule());
  // TimerController timer;
  //
  setUp(() {
    //     timer = TimerModule.to.get<TimerController>();
  });

  group('TimerController Test', () {
    //   test("First Test", () {
    //     expect(timer, isInstanceOf<TimerController>());
    //   });

    //   test("Set Value", () {
    //     expect(timer.value, equals(0));
    //     timer.increment();
    //     expect(timer.value, equals(1));
    //   });
  });
}
