import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:highvibe/app/donations/donations_page.dart';

main() {
  testWidgets('DonationsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DonationsPage(title: 'Donations')));
    //  final titleFinder = find.text('Donations');
    //  expect(titleFinder, findsOneWidget);
  });
}
