import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:highvibe/app/donations/donations_controller.dart';
import 'package:highvibe/app/donations/donations_module.dart';

void main() {
  initModule(DonationsModule());
  // DonationsController donations;
  //
  setUp(() {
    //     donations = DonationsModule.to.get<DonationsController>();
  });

  group('DonationsController Test', () {
    //   test("First Test", () {
    //     expect(donations, isInstanceOf<DonationsController>());
    //   });

    //   test("Set Value", () {
    //     expect(donations.value, equals(0));
    //     donations.increment();
    //     expect(donations.value, equals(1));
    //   });
  });
}
