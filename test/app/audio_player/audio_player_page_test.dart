import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:highvibe/app/audio_player/audio_player_page.dart';

main() {
  testWidgets('AudioPlayerPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AudioPlayerPage(title: 'AudioPlayer')));
    //  final titleFinder = find.text('AudioPlayer');
    //  expect(titleFinder, findsOneWidget);
  });
}
