import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:highvibe/app/audio_player/audio_player_controller.dart';
import 'package:highvibe/app/audio_player/audio_player_module.dart';

void main() {
  initModule(AudioPlayerModule());
  // AudioPlayerController audioplayer;
  //
  setUp(() {
    //     audioplayer = AudioPlayerModule.to.get<AudioPlayerController>();
  });

  group('AudioPlayerController Test', () {
    //   test("First Test", () {
    //     expect(audioplayer, isInstanceOf<AudioPlayerController>());
    //   });

    //   test("Set Value", () {
    //     expect(audioplayer.value, equals(0));
    //     audioplayer.increment();
    //     expect(audioplayer.value, equals(1));
    //   });
  });
}
