import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:highvibe/app/meditation/meditation_page.dart';

main() {
  testWidgets('MeditationPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MeditationPage(title: 'Meditation')));
    //  final titleFinder = find.text('Meditation');
    //  expect(titleFinder, findsOneWidget);
  });
}
