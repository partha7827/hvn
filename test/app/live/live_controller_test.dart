import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:highvibe/app/live/live_controller.dart';
import 'package:highvibe/app/live/live_module.dart';

void main() {
  initModule(LiveModule());
  // LiveController live;
  //
  setUp(() {
    //     live = LiveModule.to.get<LiveController>();
  });

  group('LiveController Test', () {
    //   test("First Test", () {
    //     expect(live, isInstanceOf<LiveController>());
    //   });

    //   test("Set Value", () {
    //     expect(live.value, equals(0));
    //     live.increment();
    //     expect(live.value, equals(1));
    //   });
  });
}
