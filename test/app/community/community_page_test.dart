import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:highvibe/app/community/community_page.dart';

main() {
  testWidgets('CommunityPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CommunityPage(title: 'Community')));
    //  final titleFinder = find.text('Community');
    //  expect(titleFinder, findsOneWidget);
  });
}
