import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:highvibe/app/profile/current_user/current_user_page.dart';

main() {
  testWidgets('CurrentUserPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CurrentUserPage(title: 'CurrentUser')));
    //  final titleFinder = find.text('CurrentUser');
    //  expect(titleFinder, findsOneWidget);
  });
}
