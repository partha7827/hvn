import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:highvibe/app/profile/audio/audio_page.dart';

main() {
  testWidgets('AudioPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AudioPage(title: 'Audio')));
    //  final titleFinder = find.text('Audio');
    //  expect(titleFinder, findsOneWidget);
  });
}
