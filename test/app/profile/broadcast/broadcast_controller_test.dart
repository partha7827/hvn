import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:highvibe/app/profile/broadcast/broadcast_controller.dart';
import 'package:highvibe/app/profile/broadcast/broadcast_module.dart';

void main() {
  initModule(BroadcastModule());
  // BroadcastController broadcast;
  //
  setUp(() {
    //     broadcast = BroadcastModule.to.get<BroadcastController>();
  });

  group('BroadcastController Test', () {
    //   test("First Test", () {
    //     expect(broadcast, isInstanceOf<BroadcastController>());
    //   });

    //   test("Set Value", () {
    //     expect(broadcast.value, equals(0));
    //     broadcast.increment();
    //     expect(broadcast.value, equals(1));
    //   });
  });
}
